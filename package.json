{
  "name": "linkedin-x",
  "version": "0.1.0",
  "private": true,
  "description": "Scuttlebutt Desk — a dashboard of senior ex-employees & stakeholders of any company, for buy-side primary research.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "psql \"$DATABASE_URL\" -f ./migrations/0001_init.sql",
    "db:seed": "psql \"$DATABASE_URL\" -f ./seed/sample.sql"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
